{
  "name": "armypose_backend",
  "version": "1.0.0",
  "description": "the backend logic for armypose application",
  "main": "new_main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . ArmyPose --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName='ArmyPose app'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^22.0.2",
    "electron-packager": "^17.1.1",
    "request": "*",
    "request-promise": "*"
  }
}
